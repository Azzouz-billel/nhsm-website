# Core
Django==5.1.15
djangorestframework==3.17.1
django-environ==0.14.0
Pillow==12.2.0
whitenoise==6.12.0
gunicorn==23.0.0

# Production-only (Postgres + optional S3). Safe to install everywhere.
psycopg[binary]==3.2.3
django-storages==1.14.4
boto3==1.35.36

# Media hosting — avatars persist across deploys. Activated in production
# only when CLOUDINARY_URL is set (see config/settings/production.py).
cloudinary==1.44.2
django-cloudinary-storage==0.3.0
